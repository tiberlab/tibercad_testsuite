# the checks to perform:

# format is:
# check = filename : variable1(precision) variable2(precision) ...
#
# If you omit any vaiables, then it will just check
# if the file has been generated.
#
# You can define more than one check
#
# examples:



check = str_msh.vtu : Strain Stress Displacement

check = dd_msh.vtu :  Ec(1e-3)  Ev(1e-2)  ElField(1) Polarization
  
check = quantum_el.dat : EigenEnergy(1e-3)
check = quantum_hl.dat : EigenEnergy(1e-3)

check = quantum_el_msh.vtu:  ProbabilityDensity(1e2)
check = quantum_hl_msh.vtu: ProbabilityDensity(1e6)
